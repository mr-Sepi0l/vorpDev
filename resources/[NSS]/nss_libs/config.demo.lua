-- This is the main config.
-- Important: Ignore this file if you are no developer that is using nss_libs for its own scripts.
-- Important: Each module can have a config.lua, too.

if not Config then
    Config = {}
end